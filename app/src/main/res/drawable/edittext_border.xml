<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@android:color/transparent" />
    <stroke
        android:width="1dp"
        android:color="@color/gray" /> <!-- 定义边框颜色 -->
    <corners android:radius="8dp" /> <!-- 定义圆角半径 -->
</shape>
